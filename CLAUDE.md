# ふみきりマップ - 開発ガイド

## このプロジェクトは何か
子どもが踏切好きなので、家族で見に行く踏切を地図上で探すためのWebアプリ。
親が地名を入力 → 周辺の踏切を地図とリストに表示 → 各踏切のストリートビュー/地図リンクを開く。

## 現在の構成(PWA化に伴いマルチファイル)
- `index.html` — 本体(UI/ロジック)
- `manifest.webmanifest` — PWAマニフェスト
- `sw.js` — Service Worker(HTMLはnetwork-first、静的物はcache-first、API はキャッシュしない)
- `icon-192.png` / `icon-512.png` — アプリアイコン(`gen-icons.py` で標準ライブラリのみで再生成可)
- **地図**: Leaflet + CartoDB dark_matter タイル(ダーク基調・無料・APIキー不要)
- **地名→座標**: Nominatim API(OSM・無料・キー不要)
- **踏切データ**: Overpass API。`railway=level_crossing`(車道用)+ `railway=crossing`(歩行者用)を取得。さらに踏切ノードが乗る鉄道wayの`name`から**路線名**も付与(無料・キー不要)
- **ストリートビュー/地図**: Googleの公開URL(`?q=&layer=c&cbll=lat,lon`)に外部リンク。**APIキー不要**

## 外部API保護(クライアント側のみ)
- 同時実行ガード(`inFlight`)・最小間隔スロットル(`MIN_GAP`=1100ms)・セッション内キャッシュ(geo/踏切)・スライダー再検索のデバウンス
- ※ 完全クライアントサイドなのでボットの直接アクセスは止められない。広く公開する場合はサーバ側プロキシ(レート制限+キャッシュ)が必要

## 設計上の重要な決定(変えるとき要相談)
- Googleのキー課金を避けるため、ストリートビューは「埋め込み」ではなく「外部リンク」方式
- すべて無料枠/キー不要で完結させるのが基本方針
- 検索半径は1〜5kmスライダーで可変(初期値2km)。スライダーを離すと直前の地点で自動再検索

## 既知の制約
- Overpass APIはレート制限あり(連続検索で一時的に弾かれる→数秒待つ)
- OSM未登録の踏切は表示されない(地方や一部私鉄で抜けの可能性)
- Nominatimも利用ポリシーあり(過剰連打しない)
- 路線名はOSMの鉄道wayに`name`が無いと出ない(出ない踏切もある)

## やりたいことリスト(優先度順)
1. ~~検索半径スライダー(1〜5km)を追加~~ ✅ 実装済み
2. ~~「現在地から探す」ボタン(Geolocation API)~~ ✅ 実装済み(※現在地機能は https/localhost のみ。file:// では動かない)
3. 行った踏切に「見たよ!」マークを付けて記録 — ※ユーザー判断で見送り(不要)
4. (任意・キー取得後)Google Street View Static APIでカード内にサムネ表示
5. お気に入り踏切のブックマーク機能

### 実装済みの追加機能
- 歩行者用踏切(`railway=crossing`)も取得し、地図ピン形状(車道=塗りダイヤ/歩行者=中抜きダイヤ)とカードのチップで区別
- 路線名・踏切名の表示(リストカード/ポップアップ)
- PWA化(ホーム画面に追加・オフライン起動)
- 外部API保護(上記セクション)
- **OSMへの踏切報告機能**(地図右上「＋ 踏切を報告」)
  - 地図タップ→ドラッグ可能なアンバー色ピンで正確な位置を指定
  - スマホ対応のため2段UI:最初は下部パネルを`compact`(位置調整・地図が見える)→「次へ」でフォーム展開
  - フォーム:種別(歩行者用/車道用)・路線(入力)・遮断機(有/無)・警報機(有/無)・踏切名(任意)→ OSMノート本文を自動生成
  - **アプリからは書き込まない**。生成メモをクリップボードへコピーし、`https://www.openstreetmap.org/note/new#map=18/lat/lon&layers=N` を開く導線(スパム責任なし・キー不要)
  - ※ OSMノートURLは本文の自動入力に非対応 → 「コピー→貼り付け」方式が前提

## デザイン方針(2026-05 更新:鉄道テーマ→洗練ダークUIへ転換)
- Palantir(Foundry/Gotham)系の洗練されたダークUIを基調にする
- 配色: 漆黒に近い背景 + 階層的なサーフェス + ヘアライン境界線。アクセントは寒色の青(`#4da3ff`)を一点だけに限定
- タイポグラフィ: 細めのウェイト、ラベルは大文字+字間広め。座標・距離・件数など数値データは等幅フォント(`--mono`)で表示
- 絵文字・装飾的アイコンは使わない。マーカーやインジケータは幾何形状(発光ダイヤモンド等)で表現
- 旧方針(子ども向けの大きい文字・親しみやすい配色・鉄道テーマ)は廃止

## コーディング方針
- 複雑化したらViteでReact化を検討(PWA化でマルチファイルになったがビルドは不要)
- 外部APIは必ずtry/catchでエラーハンドリング

## 動作確認
- **必ずローカルHTTPサーバ経由で開く**: `python3 -m http.server 8000` → `http://localhost:8000/`
- `file://` ではSafariがAPI通信(fetch)をブロックする/Service WorkerとGeolocationも動かない。Chromeのfile://でも現在地・PWAは不可
- ビルドは不要(静的ファイルをそのまま配信)

## 公開・デプロイ
- 公開URL: https://shuuu9090.github.io/fumikiri-map/ (GitHub Pages・https)
- リポジトリ: `github.com/shuuu9090/fumikiri-map`(main / root を Pages ソースに設定)
- **更新手順**: `git add -A && git commit -m "..." && git push` → push で自動再ビルド・反映(数十秒〜数分)
- 認証は gh CLI(キーチェーン)で設定済み。`git push` でPAT再入力は不要
- `.claude/` は `.gitignore` で除外(公開しない)
